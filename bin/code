#!/bin/sh

env JAVA_HOME=$SDKMAN_CANDIDATES_DIR/java/current OPENSSL_CONF=$XDG_CONFIG_HOME/openssl.cnf /usr/bin/code \
	--new-window \
	--no-sandbox \
	--enable-proposed-api GitHub.copilot \
	--proxy-server="socks5://127.0.0.1:1080" \
	"$@"
